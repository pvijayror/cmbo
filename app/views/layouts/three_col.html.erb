<!DOCTYPE html>
<html>
   <%= render 'refinery/head' %>
  <body>
   <%= render 'header' -%>
   <div class="container">
      <div class="content">
         <%= yield %>
      </div>

     <%= render 'footer' -%>
  </div>
    <!-- JavaScripts -->
   <%= javascript_include_tag 'application' %>
   <%= yield(:javascripts) %>
   </body>
</html>